@model mvc.DAL.Models.ApplicationUser
@{
    ViewData["Title"] = "Admin Dashboard";
}

<div class="text-center mb-4">
    <h1>@Model.Email</h1>
    <p class="text-muted">
        This is your central hub for managing the entire system. As an administrator, you have full access to all features, 
        including user management, product oversight, and system settings. Monitor activity, make changes, 
        and ensure the platform runs smoothly—all from one place.
    </p>
</div>

<div class="d-flex flex-column align-items-center">
    <div class="row">
        <div class="d-flex justify-content-center flex-column align-items-center p-3">
                <a asp-controller="Product" asp-action="Index" class="btn btn-warning">Manage products</a>
                <a asp-controller="Admin" asp-action="ListUsers" class="btn btn-info">Manage Users</a>
            @*  <a asp-controller="Review" asp-action="ListReviews" class="btn btn-info">Manage Reviews</a> *@
        </div>
    </div>
</div>